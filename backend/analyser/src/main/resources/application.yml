spring:
    profiles:
        active: dev

    application:
        name: analyser

    datasource:
        url: jdbc:postgresql://[HOST]:5432/postgres?user=[USER]&password=[PASSWORD]

    jpa:
        show-sql: true

api:
    keys:
        emailHunter: //[API KEY] 

google:
    gmail:
        maxResults: 200